# deps
node_modules/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.yarn/install-state.gz

# build
dist/
.next/
out/
*.tsbuildinfo
.turbo/

# data & logs
data/
*.db
*.db-*
logs/
*.log

# env
.env
.env.*
!.env.example

# os / editor
.DS_Store
.idea/
.vscode/

# swift / xcode — generated by XcodeGen / built by SwiftPM (builds go to /tmp via the Makefile)
apps/**/.build/
apps/**/.swiftpm/
apps/Binsight.xcodeproj/
apps/**/*.generated.plist
apps/**/*.xcuserstate
DerivedData/

# agent / scratch (kept out of the public repo)
.claude/
.serena/
**/AGENTS.md
**/CLAUDE.md

# internal docs & planning — kept local, not for the public repo
docs/
TODO.md

# spike / validation scripts (scratch + local .secrets.env — NEVER committed)
scripts/

# local research dumps (scratch, never committed)
lpagent_*
