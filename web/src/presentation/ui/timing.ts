/** Shared UI timing constants (milliseconds). */

/** How long a "copied!" confirmation stays before reverting to its idle label. */
export const COPIED_RESET_MS = 1500;
/** Debounce the search box so typing doesn't fire a request per keystroke. */
export const SEARCH_DEBOUNCE_MS = 250;
/** How long to wait for a test push to arrive before reporting it didn't show. */
export const PUSH_TEST_TIMEOUT_MS = 3000;
