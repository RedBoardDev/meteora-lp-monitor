name: Setup Node + Yarn
description: Install Node.js and Yarn workspace dependencies with caching

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version-file: .nvmrc
        cache: yarn
    - run: corepack enable
      shell: bash
    - run: yarn install --immutable
      shell: bash
